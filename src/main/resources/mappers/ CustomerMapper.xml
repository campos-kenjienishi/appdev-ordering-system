<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pup.taguig.app.mapper.CustomerMapper">

	<resultMap id="customerResultMap" type="com.pup.taguig.app.model.Customer">
		<id column="id" property="id"/>
		<result column="name" property="name" />
		<result column="email" property="email" />
		<result column="phone" property="phone" />
	</resultMap>
	
	<insert id="registerCustomer" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO customers
			(name, email, phone)
		VALUES 
			(#{name},
			 #{email},
			 #{phone})
	</insert>
	
	<select id="getCustomerById" resultMap="customerResultMap">
		SELECT *
		FROM customers
		WHERE id = #{id}
	</select>

</mapper>